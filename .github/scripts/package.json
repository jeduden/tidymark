{
  "name": "mdsmith-ci-scripts",
  "version": "0.0.0-dev",
  "private": true,
  "description": "CI scripts run by .github/workflows/. Bun-executed TypeScript so the repo does not have to add Python as a second runtime language.",
  "type": "module",
  "scripts": {
    "test": "bun test"
  },
  "dependencies": {
    "yaml": "2.6.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  }
}
